_schema-version: "3.2"
ID: s4requisitions
description: Generated by Fiori Tools
version: 0.0.2
modules:
- name: s4requisitions-destination-content
  type: com.sap.application.content
  requires:
  - name: s4requisitions-destination-service
    parameters:
      content-target: true
  - name: s4requisitions-repo-host
    parameters:
      service-key:
        name: s4requisitions-repo-host-key
  - name: s4requisitions-uaa
    parameters:
      service-key:
        name: s4requisitions-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: s4requisitions_html_repo_host
          ServiceInstanceName: s4requisitions-html5-service
          ServiceKeyName: s4requisitions-repo-host-key
          sap.cloud.service: s4requisitions
        - Authentication: OAuth2UserTokenExchange
          Name: s4requisitions_uaa
          ServiceInstanceName: s4requisitions-xsuaa-service
          ServiceKeyName: s4requisitions-uaa-key
          sap.cloud.service: s4requisitions
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: s4requisitions-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: s4requisitions-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - s4requisitions.zip
      name: s4requisitions
      target-path: resources/
- name: s4requisitions
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: s4requisitions-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: s4requisitions-destination-service
    service-plan: lite
- name: s4requisitions-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: s4requisitions-xsuaa-service
    service-plan: application
- name: s4requisitions-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: s4requisitions-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
